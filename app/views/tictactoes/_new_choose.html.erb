
  <% if logged_in? %>
  <%= form_for @tictactoe do |f| %>
  <%= f.hidden_field :x_id, value: @current_user.id, required: true  %>
  <%= f.hidden_field :o_id, value: @random_opponent.id, required: true %>
    <%= f.submit "Play" %>
  <% end %>
    <% end %>
